# =============================================================
# shitpost.pro - Solana Hackathon Build
# =============================================================
# SECURITY: Never commit your actual .env file to git!
# Copy this file to .env and fill in your values.

# =============================================================
# Pinata IPFS Configuration (Required for minting)
# =============================================================
# Get a free API key at https://app.pinata.cloud/developers/api-keys
# WARNING: This JWT is exposed in the browser. Use a dedicated key with
# limited permissions. For production, move uploads to a backend API.
VITE_PINATA_JWT=
VITE_PINATA_GATEWAY=gateway.pinata.cloud

# =============================================================
# Solana Program IDs
# =============================================================

# Solana Devnet
VITE_SOLANA_PROGRAM_ID_DEVNET=62gJC6oneEykVGdAq7Lr2x5bw33B3CnmHVHeeCxkZ7yJ
VITE_SOLANA_PIT_DEVNET=

# Solana Mainnet (fill in when deploying to mainnet)
VITE_SOLANA_PROGRAM_ID_MAINNET=
VITE_SOLANA_PIT_MAINNET=

# =============================================================
# Treasury Addresses (for fee collection)
# =============================================================

# Solana treasury wallet (receives SOL mint fees)
VITE_TREASURY_ADDRESS_SOLANA=

# =============================================================
# Swap Fee Configuration (0.5% fee on CoinExplorer swaps)
# =============================================================

# Solana: wSOL token account (ATA) owned by treasury wallet
# To create:
# 1. Wrap some SOL to wSOL on jup.ag with your treasury wallet
# 2. Use the wSOL ATA address here (not the treasury wallet address!)
VITE_SOLANA_FEE_ACCOUNT=

# =============================================================
# Admin Access (SECURITY CRITICAL)
# =============================================================

# Comma-separated list of Solana wallet public keys that can access /admin
# Example: VITE_ADMIN_WALLETS=ABC123...,DEF456...
# Leave empty to disable admin access entirely (recommended for production)
VITE_ADMIN_WALLETS=

# =============================================================
# Optional: Keeper Service (Buyback Bot)
# =============================================================

# WebSocket URL for burn feed (defaults to localhost:8080 in dev)
VITE_BUYBACK_WS_URL=ws://localhost:8080

# API URL for buyback stats (defaults to localhost:3001 in dev)
VITE_BUYBACK_API_URL=http://localhost:3001
